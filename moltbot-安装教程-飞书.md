# Moltbot 助手在 Windows 10 上安装并连接飞书完整教程

> **重要提示：** Moltbot 是 OpenClaw（原 Clawdbot）的新名称。本教程将指导你在 Windows 10 上安装并配置 Moltbot，连接到飞书平台。

---

## 前言

**关于安装方式的选择：**

- **方式 A（推荐）：Windows + WSL2** - 官方推荐，兼容性最好，问题最少
- **方式 B：纯 Windows（原生）** - 可行但未经过充分测试，可能遇到工具兼容性问题

**本教程以方式 A（WSL2）为主，也会提供方式 B 的说明。**

---

## 一、前置条件检查

### 1.1 Windows 10 版本要求

确保你的 Windows 10 满足以下条件：

- **版本：** Windows 10 版本 2004 或更高（内部版本 19041 或更高）
- **架构：** 64 位操作系统

**检查方法：**
1. 按 `Win + R`，输入 `winver`，回车
2. 查看弹窗中的版本信息

### 1.2 Node.js 版本要求

- **最低版本：** Node.js 22.0.0 或更高
- **推荐版本：** Node.js 22.x LTS

**检查当前版本（如果已安装）：**
```bash
node --version
```

**如果未安装或版本过低，请先安装 Node.js 22：**
- 访问：https://nodejs.org/
- 下载 LTS 版本（22.x 或更高）
- 运行安装程序，按默认选项完成安装

### 1.3 Git 安装（可选但推荐）

**检查 Git：**
```bash
git --version
```

**如果未安装：**
- 访问：https://git-scm.com/download/win
- 下载并安装 Git for Windows

---

## 二、安装方式选择与准备

### 方式 A：使用 WSL2（强烈推荐）

#### 2.1 启用 WSL2

**以管理员身份打开 PowerShell**，运行以下命令：

```powershell
# 启用 WSL
wsl --install

# 系统会提示重启，重启后会自动安装 Ubuntu
```

**重启后，完成 Ubuntu 设置：**
1. 打开 Ubuntu（会自动启动）
2. 设置用户名和密码（记住这些，后续会用到）

#### 2.2 更新 Ubuntu 并安装 Node.js 22

在 Ubuntu 终端中运行：

```bash
# 更新包管理器
sudo apt update && sudo apt upgrade -y

# 安装 Node.js 22（使用 NodeSource 官方源）
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node --version  # 应该显示 v22.x.x
npm --version
```

#### 2.3 安装 Git（Ubuntu 内）

```bash
sudo apt install -y git
```

#### 2.4 配置 npm 全局目录（避免权限问题）

```bash
# 设置 npm 全局安装路径
mkdir -p ~/.npm-global
npm config set prefix '~/.npm-global'

# 添加到 PATH（追加到 ~/.bashrc）
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
```

### 方式 B：纯 Windows 安装（未充分测试）

如果你选择不使用 WSL2，可以直接在 Windows 上安装：

#### 2.1 安装 Node.js 22（如前面 1.2 所述）

#### 2.2 安装 Git for Windows（如前面 1.3 所述）

#### 2.3 配置 npm（以管理员身份运行 PowerShell）

```powershell
# 检查 npm 配置
npm config get prefix

# 如果 prefix 指向系统目录（如 C:\Program Files\nodejs），可能需要修改
npm config set prefix "%AppData%\npm"

# 确保 npm 全局 bin 目录在 PATH 中
# 通常路径是：C:\Users\你的用户名\AppData\Roaming\npm
```

**添加到 PATH（如果尚未添加）：**
1. 右键"此电脑" → 属性 → 高级系统设置
2. 环境变量 → 在"用户变量"中找到 `Path`
3. 添加：`%AppData%\npm`

---

## 三、Moltbot 助手安装

### 3.1 运行安装脚本

**在 WSL2 Ubuntu 终端中（方式 A）或 Windows PowerShell 中（方式 B）：**

```bash
# Linux/WSL2
curl -fsSL https://openclaw.ai/install.sh | bash

# Windows PowerShell
iwr -useb https://openclaw.ai/install.ps1 | iex
```

### 3.2 验证安装

```bash
openclaw --version
```

如果显示版本号，说明安装成功！

### 3.3 运行诊断工具（可选但推荐）

```bash
openclaw doctor
```

这会检查系统配置并报告潜在问题。

---

## 四、配置 Moltbot（向导式）

### 4.1 启动配置向导

```bash
openclaw onboard
```

### 4.2 配置步骤说明

**步骤 1：免责声明**
- 选择 `Yes`，接受使用条款

**步骤 2：配置模式**
- 选择 `QuickStart`（快速入门）

**步骤 3：模型配置**
- **推荐使用国内模型提供商**（访问更稳定）：
  - **MiniMax** - https://platform.minimax.io/
  - **Qwen（通义千问）** - https://dashscope.aliyun.com/
  - **Moonshot AI（Kimi）** - https://platform.moonshot.ai/
  - **Z.AI（智谱 GLM）** - https://z.ai/manage-apikey/apikey-list

- **注意事项：**
  - OpenClaw 默认集成的是国际版接口地址
  - 如果使用上述国内模型，需要登录对应国际版控制台申请 API Key
  - 或在配置时选择国内版节点

**步骤 4：Channel 配置（飞书）**
- **重要：** 在 onboard 流程中，飞书可能不会显示
- **选择：** 先跳过这一步（稍后手动配置）
- 输入：选择 `No` 或按回车跳过

**步骤 5：Skills 配置**
- 新手建议选择 `No`，跳过技能包安装

**步骤 6：Hooks 配置**
- 选择 `Memory`（启用记忆功能，支持多轮对话上下文）

**步骤 7：完成配置**
- 等待自动部署完成（约 30 秒 - 1 分钟）
- 看到 `root@xxx:~#` 或类似提示符，表示配置完成

---

## 五、飞书应用创建与配置

### 5.1 创建飞书企业自建应用

1. **访问飞书开发者平台**
   - 网址：https://open.feishu.cn/app?lang=zh-CN

2. **登录并创建应用**
   - 登录你的飞书账号
   - 点击"创建企业自建应用"
   - 填写应用信息：
     - **应用名称：** Moltbot 助手（或其他名称）
     - **应用图标：** 上传或选择图标
   - 点击"创建"

3. **添加机器人能力**
   - 在左侧导航栏，点击"添加应用能力"
   - 选择"机器人"，点击"添加"

### 5.2 获取应用凭证

1. **进入凭证页面**
   - 左侧导航栏 → "凭据与基础信息"

2. **复制并保存凭证**
   - 复制 `App ID` 和 `App Secret`
   - **保存到记事本**（稍后会用到）
   - 不要关闭飞书开发者平台页面

---

## 六、添加飞书 Channel 到 Moltbot

### 6.1 添加飞书 Channel

**在终端中运行：**

```bash
openclaw channels add
```

**交互步骤：**
1. 选择 `Yes`（添加 channel）
2. 选择 `飞书`（Feishu）
3. 输入 `App ID`（粘贴前面保存的 ID）
4. 输入 `App Secret`（粘贴前面保存的 Secret）
5. 选择 `Finish`，完成配置

### 6.2 启动 Moltbot Gateway

**前台运行（用于测试）：**

```bash
openclaw gateway --port 18789 --verbose
```

**后台运行（生产环境）：**

```bash
openclaw gateway start
```

### 6.3 验证飞书连接状态

在飞书开发者平台中：
1. 导航到"事件与回调"
2. 查看是否显示"长连接已建立"
3. 如果提示"未建立长连接"，检查 App ID 和 App Secret 是否正确配置

---

## 七、飞书应用权限与事件配置

### 7.1 事件配置

1. **进入事件配置**
   - 飞书应用管理页 → "事件与回调"

2. **选择长连接**
   - 订阅方式选择"使用长连接"
   - 点击"保存"

3. **添加事件**
   - 点击"添加事件"
   - 选择"消息与群组"分类
   - 勾选"接收消息"
   - 点击"确定"

### 7.2 权限配置

1. **进入权限管理**
   - 左侧导航栏 → "权限管理"

2. **批量导入权限**
   - 点击"批量导入权限"
   - 复制以下 JSON 并粘贴：

```json
{
  "scopes": {
    "tenant": [
      "contact:user.base:readonly",
      "im:chat",
      "im:chat:read",
      "im:chat:update",
      "im:message",
      "im:message.group_at_msg:readonly",
      "im:message.p2p_msg:readonly",
      "im:message:send_as_bot",
      "im:resource"
    ],
    "user": []
  }
}
```

3. **点击"导入"**，等待导入完成

### 7.3 发布应用

1. **进入版本管理**
   - 左侧导航栏 → "版本管理与发布"

2. **创建并发布版本**
   - 点击"新建版本"
   - 填写版本号（如 1.0.0）和描述
   - 点击"保存"
   - 点击"申请发布"或"发布"

---

## 八、测试验证

### 8.1 验证飞书接入

1. **打开飞书 APP**
   - 使用与飞书开发者平台相同的账号登录

2. **进入工作台**
   - 在飞书首页，找到"工作台"
   - 在工作台列表中找到"Moltbot 助手"应用

3. **发起测试对话**
   - 点击应用，会自动打开私聊窗口
   - 发送测试消息，如："你好"、"帮我查询今天日程"

4. **验证结果**
   - 如果收到 Moltbot 的回复，说明接入成功！🎉

### 8.2 常用验证命令

```bash
# 查看状态
openclaw status

# 健康检查
openclaw health

# 查看日志（如果需要调试）
openclaw gateway logs
```

---

## 九、常见问题排查

### 问题 1：安装后 `openclaw` 命令未找到

**原因：** npm 全局 bin 目录未在 PATH 中

**解决方法：**

**WSL2/Linux:**
```bash
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
```

**Windows:**
1. 检查 npm 全局路径：`npm config get prefix`
2. 将 `{prefix}\bin` 添加到系统 PATH
3. 重新打开 PowerShell

### 问题 2：飞书长连接未建立

**排查步骤：**

1. **检查 App ID 和 App Secret**
   ```bash
   openclaw channels list
   ```
   确认飞书 channel 的 ID 和 Secret 是否正确

2. **检查 Gateway 是否正在运行**
   ```bash
   openclaw gateway status
   ```

3. **重启 Gateway**
   ```bash
   openclaw gateway restart
   ```

### 问题 3：模型未响应

**原因：** API Key 未配置或无效

**解决方法：**

1. **重新运行向导配置模型**
   ```bash
   openclaw configure
   ```

2. **手动配置 API Key**
   ```bash
   openclaw configure --section providers
   ```

3. **检查配置文件**
   ```bash
   # 查看当前配置
   openclaw config get
   ```

### 问题 4：WSL2 网络问题

**症状：** WSL2 无法访问外网或 DNS 解析失败

**解决方法：**

```bash
# 在 WSL2 中创建 .wslconfig 文件
# 在 Windows 中运行（PowerShell，管理员）：
notepad $env:USERPROFILE\.wslconfig
```

**添加以下内容：**
```ini
[wsl2]
networkingMode=mirrored
dnsTunneling=true
firewall=true
autoProxy=true
```

**重启 WSL2：**
```powershell
wsl --shutdown
```

然后重新打开 Ubuntu。

### 问题 5：飞书机器人无响应

**排查步骤：**

1. **检查飞书应用是否发布**
   - 确保"版本管理与发布"中已发布应用

2. **检查权限是否完整**
   - 确认所有必需的权限都已添加并发布

3. **检查事件订阅**
   - 确保"接收消息"事件已订阅

4. **查看 Gateway 日志**
   ```bash
   openclaw gateway --verbose
   ```
   观察是否有错误信息

### 问题 6：Node.js 版本过低

**解决方法：**

**WSL2:**
```bash
# 使用 nvm 安装 Node.js 22
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.5/install.sh | bash
source ~/.bashrc
nvm install 22
nvm use 22
```

**Windows:**
- 从 nodejs.org 下载 Node.js 22.x LTS
- 卸载旧版本后安装新版本

---

## 十、后续优化与建议

### 10.1 设置开机自启动

**WSL2 (systemd):**
```bash
sudo systemctl enable openclaw-gateway
sudo systemctl start openclaw-gateway
```

**Windows (使用任务计划程序):**
1. 打开"任务计划程序"
2. 创建基本任务
3. 触发器：计算机启动时
4. 操作：启动程序
   - 程序：`node`
   - 参数：`C:\Users\你的用户名\.openclaw\openclaw.mjs gateway`

### 10.2 配置更多技能包

```bash
openclaw skills list
openclaw skills add <skill-name>
```

### 10.3 启用 Web 搜索功能

1. **获取 Brave Search API Key**
   - 访问：https://brave.com/search/api/
   - 注册并获取 API Key

2. **配置 API Key**
   ```bash
   openclaw configure --section web
   ```
   输入你的 Brave Search API Key

### 10.4 启用 Control UI（Web 界面）

Gateway 启动后，访问：
```
http://127.0.0.1:18789/
```

如果配置了 Gateway Token，需要在界面设置中输入。

---

## 十一、参考资源

- **OpenClaw 官方文档：** https://docs.openclaw.ai
- **GitHub 仓库：** https://github.com/openclaw/openclaw
- **飞书开发者平台：** https://open.feishu.cn/
- **社区支持：** https://discord.com/invite/clawd

---

## 附录：完整命令速查表

| 任务 | 命令 |
|------|------|
| 安装 OpenClaw | `curl -fsSL https://openclaw.ai/install.sh \| bash` |
| 配置向导 | `openclaw onboard` |
| 启动 Gateway | `openclaw gateway --port 18789 --verbose` |
| 后台启动 | `openclaw gateway start` |
| 查看状态 | `openclaw status` |
| 健康检查 | `openclaw health` |
| 添加 Channel | `openclaw channels add` |
| 列出 Channels | `openclaw channels list` |
| 配置系统 | `openclaw configure` |
| 查看配置 | `openclaw config get` |
| 重启 Gateway | `openclaw gateway restart` |
| 停止 Gateway | `openclaw gateway stop` |
| 查看日志 | `openclaw gateway logs` |

---

**祝你安装顺利！如有问题，欢迎查阅官方文档或寻求社区帮助。** 🚀
